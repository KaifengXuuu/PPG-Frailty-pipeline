# Final Pipeline V2 agent records / V2 agent 记录

本目录只记录 `final_pipeline_v2` 的已确认决策、实施状态、待办和交接边界。
V1 是过渡版本，不得作为 V2 当前证据。所有写入遵守仓库根目录
`_agent/WRITE_RULES.md`；未验证事实必须明确标记。

推荐阅读顺序：

1. `docs/decision-log.md` — 用户确认的 V2 决策与明确 deferred 项；
2. `PROJECT_HANDOFF.md` — 当前可运行边界、禁止声明和集成状态；
3. `TODO.md` — 可执行修复队列；
4. `CHANGELOG.md` — 仅记录实际完成并经过验证的变更。

