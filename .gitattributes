# Preserve exact bytes across checkouts: configs, manifests and model artifacts
# include SHA-256-bound inputs. Do not normalize their line endings on Windows.
* -text

# CSV snapshots intentionally retain their original CRLF record separators.
*.csv whitespace=cr-at-eol
