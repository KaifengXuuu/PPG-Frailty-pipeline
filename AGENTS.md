# AGENTS.md

本文件记录所有 chat/AI agent 在本项目中必须遵守的长期规则。`_agent` 各文档的职责划分、写入边界和统一格式，以 `_agent/WRITE_RULES.md` 为准。

## 铁则

1. 所有 chat 的项目记录不可超出 `AGENTS.md` 和 `_agent` 文件夹之外。
2. 所有 `_agent` 中的更改，必须先在 chat 中展示拟写入内容，得到用户审核并明确回复“确认录入”或“同意录入”后，才可以进行更改或写入操作。
3. 每次问答后，主动向用户提供可加入 `_agent` 中一个或多个文档的内容建议，并询问是否添加。每次会话后主动提出的记录建议，需要尽量完整体现当前 chat 历史、用户历史要求、更新历史和关键上下文，确保其他 chat 可以仅通过 `AGENTS.md` 和 `_agent` 文件夹了解项目目的、项目数据结构、各模块历史版本及每次更新目的、流程进度、各模块最新版本、已实现内容、待实现内容、算法思路和可能改进方向。
4. 写入或建议写入 `_agent` 文档前，应遵守 `_agent/WRITE_RULES.md` 中定义的文档职责、写入边界和统一格式。
5. 所有 agent 必须遵守严格工作流程：先阅读 `AGENTS.md` 和 `_agent` 规则；再检查任务相关代码、数据结构和当前 git 状态；实施前说明影响范围；实施中保持更改可追溯；实施后说明已完成内容、未完成内容、验证状态和后续建议。破坏性操作、大范围重构、外部联网、写入 `_agent` 或修改 `AGENTS.md` 必须先得到用户明确确认。

6. 每次更改任何文件后，agent 必须在最终答复前展示本次实际 diff 预览并完成自审 review。普通变更展示相关 diff hunks；超大文件或自动生成文件展示 diff stat、关键 hunks、增加/删除/修改数量和完整性校验结果，不在 chat 中粘贴不可读的完整 diff。review 必须说明变更范围、与审核草稿的差异、潜在问题、验证状态和未完成内容。
